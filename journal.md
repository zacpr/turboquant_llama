# TurboQuant Integration Journal

- **setup-llama-cpp**: Cloned ggerganov/llama.cpp (depth=1) into the workspace to serve as the working tree for TurboQuant changes; baseline is clean HEAD for easier diffing and upstream rebases.
- **ggml-tq3-type (done, Mar 31)**: Added TurboQuant 3-bit codebook, FWHT helpers, quantize/dequantize routines, and registered `GGML_TYPE_TQ3_0` so CPU backend can read/write the new block layout; full CMake build succeeds (only existing warnings remain).
- **cli-turbo3-flags (done, Mar 31)**: CLI/server/completion docs now list `turbo3`; cache-type parser accepts the alias and maps it to `GGML_TYPE_TQ3_0`, plus build verified post-change.
- **cuda-tq3-kernel (done, Mar 31)**: Added warp-shuffle FWHT helpers plus 128-bit-load dequant kernels for contiguous/strided tensors and wired GGML_TYPE_TQ3_0 into CUDA get_rows so KV fetches stay on GPU; formatting target missing so clang-format not rerun.
- **turbo3-segfault-debug (done, Apr 03)**: GDB showed the crash moved from `get_rows` to `flash_attn_ext`; root cause was missing CPU `vec_dot`/quantize hooks for `GGML_TYPE_TQ3_0`. Implemented `quantize_row_tq3_0` wrapper and a reference dot-product that dequantizes blocks on the fly.
- **cuda-build-verify (done, Apr 03)**: Rebuilt llama.cpp with CUDA after the CPU fixes and reran `./build/bin/llama-cli ... --cache-type-{k,v} turbo3 -ngl 1 -p "sanity"`; the prompt now completes without segfaults on the RTX 5070 Ti.
- **cuda-tq3-convert (done, Mar 30)**: Added CUDA-side TQ3 centroid table, pack/unpack, and FWHT helpers plus a shared kernel/wrapper so GGML_TYPE_TQ3_0 converts to fp16/fp32/bf16 in both contiguous and strided cases; testing deferred—full build queued with later CUDA drops.
- **build-env-setup (blocked, Mar 31)**: Retried CUDA build with GCC 13 after `/usr/local/cuda` moved to 12.4; CMake now sees nvcc but `cuda_bf16.h` syntax errors (e.g., `struct __nv_bfloat16 __attribute__((aligned(2)))2`) break every CUDA translation unit—needs the header restored from a clean toolkit.
- **patch-cuda-headers (blocked, Mar 31)**: Our local `math_functions.h` patch succeeded but the system copy now has additional corruption in `cuda_bf16.h`, so CUDA compilation fails earlier than before; requires re-installing or carefully reapplying NVIDIA’s headers with the minimal noexcept fix.
- **patch-cuda-headers (done, Apr 04)**: Copied the clean NVIDIA `cuda_bf16.h` into `cuda-patches/include` alongside the existing `math_functions.h` override and updated `ggml-cuda` CMake to `target_include_directories(... BEFORE ...)` that folder so nvcc picks up patched headers without touching `/usr/local/cuda`.
- **build-env-setup (done, Apr 04)**: Reconfigured `build-gcc13` with `-DCMAKE_CUDA_ARCHITECTURES=86` and rebuilt successfully; runtime uses `LD_LIBRARY_PATH=/usr/local/cuda/lib64` so `libcudart.so.12` resolves without modifying the user’s environment.
- **cuda-sanity-check (done, Apr 04)**: Ran `llama-cli --cache-type-{k,v} turbo3 -ngl 1 -p "sanity"` against `tinyllama-1.1b-chat-v1.0.Q4_K_M.gguf`; prompt & generation completed on the RTX 5070 Ti, confirming the Turbo3 CUDA path now works with the local header overrides.
